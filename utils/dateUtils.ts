// Placeholder file to resolve module errors.
// Actual date formatting logic is likely handled inline or within components.

export const formatDate = (date: Date): string => {
  return new Intl.DateTimeFormat('en-US', {
    year: 'numeric',
    month: 'short',
    day: 'numeric',
  }).format(date);
};
